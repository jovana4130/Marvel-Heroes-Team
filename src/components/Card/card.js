import React from 'react';
import style from './Card.css';
/*import { Link } from 'react-router-dom';*/

/*export const Card = ({ heroes, addToMyTeam }) => {
    return (
        <div>
            {heroes.map(hero => 
                <Card   
                    key={hero.id}
                    id={hero.id}
                    avatar={hero.avatar}
                    name={hero.name}
                    addToMyTeam={addToMyTeam}
                />)}
        </div>
    );
};*/

